strategies:
  cnn_momentum:
    enabled: false
    asset_universe: ["SPY", "QQQ", "AAPL", "MSFT", "BTCUSDT"]
    model: "cnn_v3"
    signal_threshold: 0.65
    max_holding_days: 5
  macro_regime_filter:
    enabled: false
    model: "regime_classifier"
    allowed_regimes: ["expansion", "risk_on"]
promotion:
  require_backtest: true
  require_walk_forward: true
  require_paper_trade: true
